// All variables use {{VAR}} format placeholders.
// Replace values below when finalized. Use `grep -r "{{"` to find all occurrences.

export const SITE = {
  COMPANY_NAME: '{{COMPANY_NAME}}',
  SERVICE_NAME: '{{SERVICE_NAME}}',
  TEL: '{{TEL}}',
  TEL_HOURS: '{{TEL_HOURS}}',
  LINE_URL: '{{LINE_URL}}',
  EMAIL: '{{EMAIL}}',
  ADDRESS: '{{ADDRESS}}',
  AREA: '{{AREA}}',
} as const;
